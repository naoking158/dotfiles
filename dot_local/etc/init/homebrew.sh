#!/bin/bash

bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

echo "If you want to install brew packages listed in the Brewfile, do `brew bundle --global`."
